{
  "name": "src-420-317-ah-nodejs",
  "version": "1.0.0",
  "description": "Project example",
  "main": "src/main.ts",
  "scripts": {
    "start": "node dist/main.js",
    "build": "esbuild src/main.ts --bundle --outdir=dist --format=cjs --platform=node",
    "build:dev": "esbuild src/main.ts --bundle --outdir=dist --format=cjs --platform=node --watch",
    "types": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.19",
    "@types/node": "^20.8.6",
    "esbuild": "^0.19.4",
    "typescript": "^5.2.2"
  }
}
